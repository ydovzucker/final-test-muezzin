version: "3.8"

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"  # host can reach Kafka at 9092
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      # Fixed: separate ports internally for each listener
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
#  volumes:
#    kafka-data:

#  elasticsearch:
#    image: docker.elastic.co/elasticsearch/elasticsearch:8.13.4 # Use the official Elasticsearch image
#    container_name: elasticsearch
#    ports:
#      - "9200:9200" # Expose Elasticsearch HTTP port
#      - "9300:9300" # Expose Elasticsearch transport port
#    environment:
#      - discovery.type=single-node # For a single-node setup
#      - xpack.security.enabled=false # Disable security for ease of use in development (not recommended for production)
#      - ES_JAVA_OPTS=-Xms512m -Xmx512m # Set Java heap size
#    volumes:
#      - elasticsearch_data:/usr/share/elasticsearch/data # Persist data
#    ulimits:
#      memlock:
#        soft: -1
#        hard: -1
#    healthcheck:
#      test: [ "CMD-SHELL", "curl --silent --fail localhost:9200/_cluster/health || exit 1" ]
#      interval: 10s
#      timeout: 10s
#      retries: 5
#
##  volumes:
##    elasticsearch_data:
